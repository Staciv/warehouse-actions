export const ROUTES = {
  login: '/login',
  dashboard: '/',
  workerCompleted: '/worker-completed',
  actions: '/actions',
  actionDetails: '/actions/:id',
  completed: '/completed',
  vehicles: '/vehicles',
  workers: '/workers',
  carriers: '/carriers',
  actionTypes: '/action-types',
  reports: '/reports'
} as const;
